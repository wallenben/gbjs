export type registers = {
    //8-bit registers
    a: number;
    b: number;
    c: number;
    d: number;
    e: number;
    h: number;
    l: number;
    //16-bit registers
    pc: number; //program counter
    sp: number; //stack pointer

}